# Generated by openapi-typescript via `npm run contract:check` (CI) and
# `npm run generate:api` (local). The generator emits its own formatting,
# and Prettier-rewriting it causes the contract-check step to fail with a
# spurious "drift" diff.
src/services/generated/
